package io.bitnomio.attendance.app;

import org.springframework.stereotype.Service;

@Service
public class ClockOut {

  // validate if there's a clock in for today

  // if yes, then clock out

  // if no, then throw exception

}
