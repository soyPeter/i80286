input{
    tcp {
        port => 5001
        codec => json
    }
    gelf {
      port => 12201
      codec => json
    }
}
output {

  stdout { codec => rubydebug }

  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "i8086_web"
  }
}
